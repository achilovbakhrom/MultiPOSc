package com.jim.multipos.utils;

import android.app.Dialog;
import android.content.Context;
import android.graphics.drawable.ColorDrawable;
import android.os.Handler;
import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import android.util.Log;
import android.view.View;
import android.view.Window;
import android.view.animation.Animation;
import android.view.animation.AnimationUtils;
import android.widget.ImageSwitcher;
import android.widget.ImageView;
import android.widget.TextView;

import com.jim.mpviews.MpButton;
import com.jim.mpviews.MpEditText;
import com.jim.multipos.R;

import javax.inject.Inject;

/**
 * Created by DEV on 01.08.2017.
 */

public class LoginAdminDialog extends Dialog {
    private MpButton btnLogin;
    private MpEditText etAdminPassword;
    private Context context;

    public LoginAdminDialog(@NonNull Context context) {
        super(context);
        this.context = context;
        init();
    }

    public LoginAdminDialog(@NonNull Context context, int themeResId) {
        super(context, themeResId);
        init();
    }

    protected LoginAdminDialog(@NonNull Context context, boolean cancelable, @Nullable OnCancelListener cancelListener) {
        super(context, cancelable, cancelListener);
        init();
    }

    public void init() {
        requestWindowFeature(Window.FEATURE_NO_TITLE);
        setContentView(R.layout.login_dialog);
        getWindow().setBackgroundDrawable(new ColorDrawable(android.graphics.Color.TRANSPARENT));
        btnLogin = (MpButton) findViewById(R.id.btnLogin);
        etAdminPassword = (MpEditText) findViewById(R.id.etAdminPassword);
    }

    public void setOnOKClickListener(View.OnClickListener listener) {
        btnLogin.setOnClickListener(listener);
    }

    public String getPassword() {
        return etAdminPassword.getText().toString();
    }

    public boolean check() {
        if (!etAdminPassword.getText().toString().equals("") && etAdminPassword.getText().length() == 12) {
            return true;
        } else {
            etAdminPassword.setError("Enter admin password");
            return false;
        }
    }

    public void setWrong(){
        etAdminPassword.setError("Incorrect password");
    }
}












































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































